- if controller_name != "sessions"
  = link_to "Sign in", new_session_path(resource_name), class: "text-sm text-primary hover:underline"

- if devise_mapping.registerable? && controller_name != "registrations"
  = link_to "Create account", new_registration_path(resource_name), class: "text-sm text-primary hover:underline"

- if devise_mapping.recoverable? && controller_name != "passwords" && controller_name != "registrations"
  = link_to "Forgot password?", new_password_path(resource_name), class: "text-sm text-primary hover:underline"
